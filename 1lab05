#include <stdio.h>

int main(){

    float x, y;

    printf("Input X: ");
    scanf("%f", &x);

    printf("Input Y: ");
    scanf("%f", &y);

    x > y ? printf("X is greater than Y") : printf("X is less than Y");

    return 0;
}
